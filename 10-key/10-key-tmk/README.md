10-key keyboard firmware
======================

## Setup
Put this directory within the keyboard directory of [tmk_keyboard's firmware](https://github.com/tmk/tmk_keyboard).

## Build
cd into this directory and run `make`:

    $ make
